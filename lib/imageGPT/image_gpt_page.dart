import 'package:flutter/material.dart';

class ImageGPTPage extends StatelessWidget {
  const ImageGPTPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('img page'),);
  }
}